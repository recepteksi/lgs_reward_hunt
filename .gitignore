# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Widget Preview related
.widget_preview/

# Flavorizr regenerates these from the pubspec block, but the generated
# sources ARE committed: a checkout has to build without running a code
# generator first, and the native diffs are what a reviewer needs to see.

# Local IDE state
.vscode/launch.json.bak

# Build output and secrets that must never reach git
/android/build/
android/key.properties
*.jks
*.keystore
*.p8
*.mobileprovision
/ios/build/
.claude/settings.local.json

# Firebase client configuration. Not secret by Firebase's design, but the repo
# is public and the API keys inside are scanned: CI restores these six files
# from the FIREBASE_CONFIG_ARCHIVE secret (see WORKFLOW.md).
android/app/src/*/google-services.json
ios/config/*/GoogleService-Info.plist
lib/infrastructure/config/firebase/firebase_options_*.dart
