/// The three slides of the intro, in the order they are shown.
///
/// A vocabulary rather than an index, so the slide's picture and its words are
/// chosen by an exhaustive `switch` and a fourth slide cannot appear with the
/// third one's title. [emblem] is the promise, [tasks] the day, [rewards] where
/// the points go — the loop the app is, told once before the parent signs up.
enum IntroSlideEnum { emblem, tasks, rewards }
